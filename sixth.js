// Strings Methods
// Split() Method

const text = `Hello my name is Zillight`;

const result = text.split(` `);
console.log(result); // [ 'Hello', 'my', 'name', 'is', 'Zillight' ]

// splits into an array of strings, each word becoming a string in the sentence. 

const letter = `Zillight`;
const result1 = letter.split(``);
console.log(result1); // [ 'Z', 'i', 'l', 'l', 'i', 'g', 'h', 't' ]

// Note: Do not forget to put "" and don't put space inbetween them.